import React from "react";

import { ReactComponent as Logo } from "../assets/logo.svg";

const Game = ({ isP1X }) => {
  return (
    <section id="game">
      <div className="cw">
        <h2>Game</h2>
      </div>
    </section>
  );
};

export default Game;
